import { createTheme } from "@mui/material/styles";

export const theme = createTheme({
    palette:{
        primary:{
            main: "#FF9900",
            contrastText: "#00000"
        },
        secondary:{
            main: "#00000",
            contrastText: "#FFFFF"
        }
    }
})